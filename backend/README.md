# Backend

Express + MongoDB backend for the Marketing Agency project.

## Setup

1. Install dependencies:

   npm install

2. Create a `.env` file from `.env.example`.

3. Start the development server:

   npm run dev

## Health check

- `GET /health`
